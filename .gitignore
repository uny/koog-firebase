# --- Gradle ---
.gradle/
build/
**/build/

# --- Kotlin ---
.kotlin/

# --- IDE (IntelliJ IDEA / Android Studio) ---
.idea/
*.iml
*.iws
out/

# --- Local Configuration (SDK paths, etc.) ---
local.properties

# --- OS Specific ---
.DS_Store
Thumbs.db

# --- Apple / Swift PM ---
.build/
DerivedData/
StartYourBridgeHere.swift
# .swiftpm-locks/ is committed on purpose: it is the Kotlin Gradle plugin's SwiftPM lock
# (Package.resolved) and pins the transitive firebase-ios-sdk version so CI resolves the same
# graph as local builds. Its own .gitignore excludes the checkout. Only Package.resolved is
# load-bearing; the manifests and stub sources below are regenerated on every build.
.swiftpm-locks/*/swiftImport/Package.swift
.swiftpm-locks/*/swiftImport/Sources/
.swiftpm-locks/*/swiftImport/subpackages/

# --- Local working docs (not for publishing) ---
docs/local/
